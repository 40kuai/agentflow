# 记录.md

> 本项目工作记录。每次完成后更新：已完成 / 下一步 / 未完成 / 阻塞。

## 项目：AgentFlow — 多 Agent 开发流程编排平台

**目标**：让多个角色（CEO / CTO / PM / 架构师 / 开发 / 测试 / 评审 / DevOps）在开发流程中自动流转，
并由一个带事件存储的编排内核持有唯一状态真相；通过 Web 页面实时观测任务进度与流转。

---

## 环境基线（已核实）

| 项 | 版本 / 状态 |
| --- | --- |
| 工作目录 | `/Users/40kuai/Documents/多agent开发流程`（已 `git init`） |
| Node | v22.22.3 |
| npm | 10.9.8 |
| Python | 3.9.6（系统自带，**不修改、不安装包**） |
| git | 2.50.1 |
| Claude Code CLI | 2.1.38，`/opt/homebrew/bin/claude` |
| Codex CLI | 已安装，`/opt/homebrew/bin/codex` |
| 其他 | docker、tmux 可用 |

### CLI 关键能力（已用 `--help` 核实，非推测）

claude：
- headless：`-p/--print`
- 流式：`--output-format stream-json --include-partial-messages`
- 强制结构化输出：`--json-schema <schema>`
- 单次预算上限：`--max-budget-usd <amount>`
- 会话：`--session-id <uuid>` / `--resume` / `--fork-session` / `--no-session-persistence`
- 提示词：`--system-prompt` / `--append-system-prompt`
- 工具权限：`--tools` / `--allowed-tools` / `--disallowed-tools` / `--permission-mode`
- 工作区：`--add-dir`
- 模型：`--model` / `--fallback-model` / `--effort`

codex：
- headless：`codex exec`（别名 `e`），prompt 可走参数或 stdin
- 内置评审：`codex review --base <branch>` / `--uncommitted`
- 沙箱：`-s read-only|workspace-write|danger-full-access`
- 工作区：`-C/--cd <DIR>`
- 配置档：`-p/--profile <name>`、`-c key=value`
- 会话续跑：`codex exec resume`
- 可作为 MCP server：`codex mcp-server`

---

## 已确认的关键决策

| 决策项 | 结论 |
| --- | --- |
| 总方案 | 事件驱动的编排内核（方案 A），非黑板模式、非中心化对话编排 |
| 执行引擎 | 复用 Claude Code / Codex CLI（headless 调用），可插拔适配器 |
| 流转决策 | 混合：规则优先 → LLM 兜底（受限枚举）→ 人工卡点 |
| 信息流转 | 角色间**零自由对话**，只走三条通道：Artifact 事件总线（权威）/ git 工作区 / 受限问答 |
| 工作区隔离 | `git worktree`（非 Docker） |
| 角色集 | 完整公司角色，提供 `full` / `lean` / `solo` 三档 profile |
| 并行机制 | 工作包（Work Package）驱动 + Fan-out/Fan-in 原语 + 文件级 ownership |
| 评审角色 | 默认 2 个，**强制交叉引擎**（codex 评 claude 写的代码，反之亦然） |
| 交付范围 | 完整平台实现，按阶段推进 |

---

## 进度

### 阶段：设计（当前）

- [x] 环境核查（Node / CLI / git / Python）
- [x] CLI 能力核实（`claude --help`、`codex exec --help`）
- [x] 方案选型与角色体系设计（用户已确认）
- [x] 创建 `.gitignore`
- [x] 创建 `记录.md`
- [x] 撰写架构设计文档 spec → `docs/superpowers/specs/2026-09-18-multi-agent-dev-orchestration-design.md`
- [x] spec 自查（修掉 5 处内部不一致：交叉评审引擎冲突、profile 角色数矛盾、deps() 未定义、原则二与受限问答的表述张力、目录缺 spikes）
- [x] 提交 git（commit `54ef9d5`）
- [x] 用户评审 spec（已批准）
- [x] 撰写实现计划 → `docs/superpowers/plans/2026-09-18-agentflow-phase0-phase1.md`（15 个 Task，覆盖 Phase 0 + Phase 1）
- [x] 计划自查（修掉：两处"先给错版再给修正版"的步骤、4 处前后类型/命名不一致、2 处测试断言与实现不匹配、3 处无效代码残留）
- [x] **选择执行方式并开始实现**（Subagent-Driven）
- [x] Task 1：项目脚手架与配置加载
- [x] Task 2：Phase 0 CLI 行为探针
- [x] **Task 3：共享领域类型与 Artifact schema**（6 个文件，11/11 测试通过，`tsc --noEmit` 干净）
- [x] **Task 4：事件存储（append-only）**（`src/kernel/event-store.ts` + 测试，评审后修复 3 处：校验前置/返回值重读/补 `readAll` 与空库 `lastSeq` 覆盖，现 9/9 通过，全量 22/22，`tsc --noEmit` 退出码 0）
- [x] **Task 5：事件重放投影器**（`src/kernel/projector.ts` + 测试，11/11 通过，commit `74b5167`）
- [x] **Task 6：受限表达式求值器 + 事实构建**（`src/kernel/expression.ts`、`src/kernel/facts.ts` + 测试，评审后修复 2 Important + 1 Minor，commit `26adaa6`/`d19b2ef`）
- [x] **Task 7：状态机（转移求值）**（`src/kernel/state-machine.ts` + 测试，评审裁决落地：死循环保护语义注释 + 钉固用例，12/12 通过，全量 62/62，commit `ea404b2`）
- [x] **Task 8：Runner 接口与 Fake Runner**（3 个文件，5/5 测试通过，全量 67/67，`tsc --noEmit` exit 0，commit `9ccfd20`）
- [x] **Task 8 评审后修复**（Important：队列耗尽抛错 + Minor：`FakeScriptItem` 由 `RunnerEvent` 派生；6/6、全量 68/68，commit 见下）
- [x] **Task 9：上下文装配器**（`src/kernel/context-assembler.ts` + 测试，7/7；评审后回退多余的 `worktreePath` 字段并删除不可达分支）
- [x] **Task 10：Claude Code Runner 适配器**（6 个文件，超时用例 306ms，全量 90/90，`tsc --noEmit` exit 0；含 2 处对简报的必要修正，见「最近更新」）
- [x] **Task 10 评审后修复（2 处 Important：护栏确定性 + 父进程退出清理）**（超时用例 585~604ms、连跑 5 次全绿；变异 5/5 稳定变红；全量 90/90；无残留进程，见「最近更新」）
- [x] **Task 10 gate 收尾（凭据恢复后补跑）**：`--json-schema` 默认值由「不传」反转为「传」；解析层改读 `result.structured_output` 优先；真实 Runner 端到端双路径实测（全量 95/95）
- [x] **Task 11：配置加载（角色与工作流）**（`src/config/{schema,loader}.ts` + 2 个测试 + `config/` 下 3 prompt/3 role/1 workflow，共 11 个文件；10/10 通过，全量 105/105，`tsc --noEmit` exit 0）
- [x] **Task 12：调度器与内核主循环**（`src/kernel/{scheduler,kernel}.ts` + `kernel.test.ts`，源码与简报逐字一致；14/14 通过，全量 121/121，`tsc --noEmit` exit 0；含 2 处对简报夹具的必要修正 + 1 条自补用例，见「最近更新」）
- [x] **Task 13：HTTP + WebSocket 服务与进程入口**（`src/server/{server,server.test}.ts` + `src/main.ts`，7/7 新用例，全量 128/128，`tsc --noEmit` exit 0；首次完成全系统接线；含 1 处对简报夹具的必要补充，见「最近更新」）
- [x] **Task 14：极简观测页面**（`web/` 独立前端包，8 个文件；`npm install --registry=https://registry.npmmirror.com` 装 67 包，`tsc --noEmit` exit 0，`vite build` 成功产出 `web/dist/`（152K，32 modules），commit `604b8a3`）
- [x] Task 15：端到端冒烟 —— **最终 PASS**（2026-09-19 权限修复后重跑 1 次）：`status=completed`、**3/3 节点** `node.succeeded`、
  **3/3 artifact**（`requirement` / `code_diff` / `test_report`，均 `status: ok`）、`dev_implement` 真实产出 **`self_test_result=passed`**、
  `requires approval` 由首轮 **461 次降到 7 次**、**实测总花费 $0.5876586**（全程 213.4s）。
  **首轮（连跑 2 次）FAIL 的原始数据与根因（headless 下 Bash 命令被自动拒绝 + `smoke.sh` 的 `sed` 贪婪缺陷）保留在
  「Phase 1 实测结果」的「首轮 FAIL 记录」小节，不删除** —— 那是本项目最有价值的记录之一。详见「Phase 1 实测结果」
- [x] **headless 权限修复（精准预授权）**：runner 改 `buildArgs`（可写角色追加 `--allowed-tools`，只读角色不放宽）、
  冒烟脚本修贪婪 `sed` 缺陷、新增 3 条 args 用例（全量 131/131、`tsc --noEmit` exit 0）。6 次真实探针共 **$0.374775**，
  详见「最近更新（2026-09-19 权限修复）」与 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-perm-fix-report.md`
- [x] **headless 权限评审补洞（3 项 Important）**：注释如实标注「shell 前缀 ≡ 任意执行 / git·npm 授权面过宽」，
  只读角色边界补 `not.toContain('dangerously-skip-permissions')` + 整串精确断言，计划文档 Task 10 段补「实测修正」块。
  变异验证：给只读分支加该参数后**新断言变红（2 failed）**；全量 **132/132**、`tsc --noEmit` exit 0。
  详见「最近更新（2026-09-19 权限评审补洞）」与 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-perm-audit-report.md`
- [x] **端到端重跑验证（权限修复是否真的让链路跑通）**：`bash tests/e2e/smoke.sh` 实测 **PASS**
  （`status=completed`、3/3 `node.succeeded`、3 个 artifact、3/3 `log_ref` 存在），全程 **213.4s**、
  **实测总花费 $0.5876586**（`budgetUsedUsd` 实读）；**`dev_implement` 真实产出 `self_test_result: "passed"`**，
  `requires approval` 由上一轮 **461 次降到 7 次**。详见「Phase 1 实测结果」与
  `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-15-report.md`（追加「第二轮：重跑验证」）。
- [x] **契约缺陷修复（2026-09-19，真实失败现场驱动）**：① `status` 进入 payload schema（模型在结构化输出里表达判断），
  `parseArtifactPayload` 返回 `{status, payload}`，内核不再写死 `'ok'`，边条件真正生效；
  ② `error_max_structured_output_retries` 被识别并自动降级（关 `--json-schema`）重跑一次，作为一次有代价的补救尝试（两条通道各自都可能零产出，**并非互为兜底**）；
  ③ 管理台「并行度」恒为 0 的假指标改为如实标注。**全量 157/157（20 文件，2026-09-19 残留修复轮复核实测）**、`tsc --noEmit` exit 0，两次变异验证均稳定变红。
  （注：本行原记「全量 143/143」已过时——其后 `58b4567` 新增管理台日志端点用例，本轮又补 1 条 schema 断言，故更新为 157。）
  详见下方「最近更新（2026-09-19 契约修复）」与
  `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-status-contract-fix-report.md`

### 下一步

1. ~~推进 Task 15（端到端冒烟）~~ ✅ **已通过（2026-09-19 重跑）**，结果见「Phase 1 实测结果」；**遗留 2 项**：
   (a) ~~**runner 的 Bash 权限**（headless 下非白名单 Bash 命令被自动拒绝，与 dev/qa prompt 要求「真实运行测试」直接冲突）——
   建议改用 `--allowed-tools` 预授权或等价方案，修好后重跑端到端~~ ✅ **已修复（2026-09-19 权限修复）并已复跑验证生效**：
   可写角色改为 `acceptEdits` + `--allowed-tools` 六类前缀预授权，重跑实测 dev/qa **确实执行了** `chmod` / `bash x.sh` /
   `bash -n` / `od -c` / `bash tests/*.sh`，重试与单节点成本**大幅回落**（dev 70.5s/$0.204、qa 110.0s/$0.329）。
   **残留**：`./scripts/hello.sh`（直接执行脚本、不经 `bash`）与「多操作复合命令中的非白名单片段」仍会被拒（本轮 7 次）。
   (b) ~~`tests/e2e/smoke.sh` 的轮询窗口（180×5s 且 `sed` 贪婪取到最后一个 `status` 导致早退失效）需按真实耗时放宽~~ ✅ **已修复**：
   改用 node 解析取**任务顶层** `status`，窗口放宽到 480×5s（40 分钟），并每轮打印已等待时长。
   (c) **未修复** 内核未给单节点设置预算上限（`budgetCapUsd` 从不传），单节点花费不受约束。
2. ~~Task 10 需在 claude 额度恢复后补跑带 `--json-schema` 的真实调用~~ ✅ 已完成（2026-09-18）

### 最近更新（2026-09-19 契约修复：status 由模型给出 + 结构化输出兜底）

- **真实失败现场驱动的 3 处契约缺陷修复**。一次真实任务里 `pm_analyze` 以
  `subtype: error_max_structured_output_retries` 零产出收场（22 轮 / 103 秒 / $0.4170648，`result: undefined`，
  原始日志 `logs/runs/run_46e7a5442100440da2ac.jsonl`）。**未调用真实 claude**（全部靠单测 + 逐字归档 fixture +
  假 bin 验证）、**无新依赖**、未触碰全局环境，**未改** `killTree` / `detached` / 超时 / 退出钩子 / `smoke.sh` /
  六类预授权串，未重构 `ARTIFACT_PAYLOAD_SCHEMAS` 的形状，未改动 3 个原有归档 fixture。
  1. **缺陷 1（status 契约）**：spec §5.3 与 role prompt 都要求模型判断 `status`，但 payload schema 里没有它，
     模型只能把 `blocked` 写进 `problem` 的散文里；内核又恒写 `status: 'ok'`，导致边条件
     `all(artifacts.requirement.status == 'ok')` 形同恒真，**模型的语义判断被静默吃掉**。
     修法（增量）:4 个 payload schema 各加 `status: ArtifactStatusSchema.default('ok')`
     （`src/shared/artifacts.ts`，`default` 用于兼容归档 fixture——它们没有该字段）；
     `parseArtifactPayload` 返回 `{status, payload}` 并把 status 从 payload 中剔除；
     内核 `artifact.created` 写 `parsed.status`（`src/kernel/kernel.ts:322`）；
     `decideNext` 无可用转移时的失败原因补「当前产物状态：requirement=blocked」
     （`src/kernel/state-machine.ts:93-101`），错误信息因此能直接说明原因。
     `config/prompts/pm.md:10` 改为「用 status 字段判为 blocked，并在 problem 里写清具体歧义点」。
     **变异验证**：把内核改回写死 `status: 'ok'` → 新用例**恰好 1 条变红**（`1 failed | 15 passed`），还原后绿。
  2. **缺陷 2（retry 耗尽零产出）**：该次调用里 **5 次 `StructuredOutput` 全部返回 "provided successfully"、
     提交的键集合恰好只有 schema 要求的 4 个键**，CLI 仍判 `error_max_structured_output_retries` ——
     说明 harness 要求那次提交是**终结动作**，模型在调用之间继续 Grep/Read 并重复提交，最终耗尽预算。
     修法两处：① 装配 prompt 的「硬约束」新增第 5 条终止语义（`src/kernel/context-assembler.ts:93-98`）；
     ② `src/runner/claude-code-runner.ts` 新增 `isStructuredOutputRetriesExhausted(line)`，
     并在 `createClaudeCodeRunner` 外层包一层：识别到该 subtype 时**自动关闭 `--json-schema` 重跑一次**
     （走 `result` 通道；只降级一次、失败即按失败处理、只读角色不做例外、留一条 `{kind:'log'}` 说明、
     拦下首次尝试的 `exited` 以免内核把它的退出码当最终结果）。
     真实 `result` 行**逐字**归档为 `tests/fixtures/claude-stream-maxretries-sample.jsonl`
     （已用 node 比对 `wrapper.chunk === fixture 内容` 逐字相等），契约测试断言该样本必须被识别为「需降级重试」。
     **变异验证**：去掉降级（`canFallback = false`）→ 降级重试用例**2 条变红**，还原后绿。
     ⚠️ **代价（预期内）**：降级是**一次有代价的补救尝试**，而非「两条通道保证互为兜底」——
     归档证据 `claude-stream-plain-sample.jsonl` 恰恰说明关掉 schema 的通道会以「模型把 JSON 包进代码块 →
     `JSON.parse` 失败」的方式零产出，故降级可能只是从一种零产出模式换到另一种（此时代码块路径会显式失败，不是静默）。
     另：降级会新增一次真实调用与额外花费，且单节点的 wall-clock 超时与 `--max-budget-usd` 预算上限**实际为标称值的 2×**
     （两次尝试各起一个 timeoutTimer、各带一次预算上限，有界且已知）。
  3. **缺陷 3（假指标）**：管理台「并行度」（取 `currentNodeIds.length`）**恒为 0**，因为内核是单节点串行
     （`src/kernel/kernel.ts` 的 `await runNode(...)`）。已选择**如实标注**而非移除：
     `web/src/App.tsx:121-122` 改为「并行度 N（Phase 1 串行执行，并发调度属 Phase 2）」——
     理由是保留该槽位可让读者一眼看到「这里有东西但尚未实现」，比直接删掉更不易被误读为「已支持并发」；
     **未实现并发调度本身**。`env.globalConcurrency` 无消费者一事已登记进「未完成 / 待验证」表。
  - **TDD**：先 RED 后 GREEN —— 缺陷 1 RED = `5 failed | 22 passed`；缺陷 2 RED = `5 failed | 10 passed`。
  - **验证**：全量 `npx vitest run` 与 `npx tsc --noEmit` 结果见报告；
    3 个原有归档 fixture 的解析测试仍通过（证明增量方案没有破坏向后兼容）。
  - **提交**：`4fe53c2`（status 契约）、`3354017`（降级重试）、本次提交（假指标 + 文档同步）。
  - 报告：`.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-status-contract-fix-report.md`。

### 最近更新（2026-09-19 残留修复轮：status 必填 + started 契约 + 措辞/计数订正）

- **承接契约修复波的评审「有条件接受」，收尾 1 项 Important + 5 项 Minor**。未调用真实 claude、无新依赖、
  未触碰全局环境、未改 `killTree`/`detached`/超时机制/退出钩子/`smoke.sh`/六类预授权串/降级触发条件，也未改 4 个归档 fixture。
  1. **Important（`status` 的 `default('ok')` 留下残余静默成功路径）**：4 个 payload schema 的 `status` 带 `.default('ok')`
     且不在 `required` 里，故签发到 CLI 的 JSON Schema 不强制模型给出该字段——漏给即被当 `ok`，边条件
     `all(artifacts.*.status == 'ok')` 直接放行，语义上回到「判断被静默吃掉」。修法：zod 侧**保留** `.default('ok')`
     （归档 fixture 旧形状仍可解析），在 `jsonSchemaForArtifact`（`src/shared/artifacts.ts:124-141`）把 `status`
     注入输出 JSON Schema 的 `required`，对 **4 个类型**统一生效；该产物同时用于签发 `--json-schema` 与内联进装配 prompt
     （`src/kernel/context-assembler.ts:82`），两处一致；展开原 schema 保留 `additionalProperties: false`。
     断言：`src/shared/artifacts.test.ts` 新增「4 个类型的 required 均含 status 且保留 additionalProperties: false」。
     **变异验证**：临时去掉注入 → 新断言**恰好 1 条变红**（`1 failed | 11 passed`，报 `requirement 的 required 应包含 status`），还原后绿。
  2. **Minor 1（降级时第一次尝试的 `started` 泄漏）**：`src/runner/claude-code-runner.ts` 的 wrapper 现缓冲第一次尝试的
     `started`，降级时连同作废的尝试一起丢弃，只发重试那条 → 恢复「一次 run 一个 started」。关闭 schema（不可能降级）时
     仍即时发出以保留原事件顺序；开启 schema 时 started 略晚于中间事件发出（内核忽略 `started`，无功能影响）。
     断言：`claude-code-runner.retry.test.ts` 两个降级用例均断言 `started` 恰好 1 条、且位于降级说明之后
     （证明留下的是重试那次的）；`timeout` / `spawn-error` 用例（不含降级路径）仍绿。
  3. **Minor 2（单节点时间/预算上限实际 2×）**：已在 `claude-code-runner.ts` 的 `createClaudeCodeRunner` 文档注释与
     计划文档 Task 10 段如实说明（两次尝试各起一个 timeoutTimer、各带一次预算上限，有界且已知）。
  4. **Minor 3（「互为兜底」措辞过强）**：代码注释、计划文档 Task 10 段、本记录一并改为「降级是一次有代价的补救尝试，
     不保证产出」，并点明 `claude-stream-plain-sample.jsonl` 证实的另一种零产出模式。
  5. **Minor 4（测试计数不一致）**：本文件原记「143/143」已过时，订正为 **157/157（20 文件）**（说明见上文该项）。
  6. **Minor 5（`projector.ts` 读侧 `str(p,'status','ok')`）**：加注释标注为**历史兼容缺省、非活路径**
     （内核必写 `parseArtifactPayload` 提升出的 `parsed.status`），保留缺省以容忍更早的历史事件。
  - 验证：全量 `npx vitest run` = **157/157（20 文件）**、`npx tsc --noEmit` exit 0；3 个归档 fixture 的解析测试仍通过。
  - 报告：`.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-residual-fix-report.md`。

### 最近更新（2026-09-19 整分支终审修复）

- **本分支最后一次修复波次**（终审结论「修完 3 项后可合并」）：修掉 **1 项 Critical + 4 处文档不同步**。
  **未改任何授权参数、未调用真实 claude、无新依赖、未触碰全局环境**。
  1. **Critical（进程级崩溃）**：`src/runner/claude-code-runner.ts` 的 `child.on('error', ...)` 原先注册在第一个
     `yield { kind: 'started' }` **之后**；spawn 失败（ENOENT）的 `'error'` 经 `process.nextTick` 投递，
     先于该 yield 的微任务续跑到达 → `Unhandled 'error' event` → **整个 Node 进程崩溃**
     （真实 `main.ts` + 一次 `POST /api/tasks` 即可复现，任务永久卡在 `active`）。
     已把「`queue` / `push` 声明 + error 监听」整体前移到 `logStream.write(...)` 与 `yield` **之前**
     （`src/runner/claude-code-runner.ts:262-283`，`child.on('error', …)` 在 `:272`、`yield` 在 `:283`），并新增回归测试 `src/runner/claude-code-runner.spawn-error.test.ts`。
     **变异验证**：把监听挪回 yield 之后 → 该用例**变红**（10s 超时 + vitest 报 `Uncaught Exception: spawn /nonexistent/claude ENOENT`）；还原后绿。
     **真实路径 A/B 验证**（`AGENTFLOW_CLAUDE_BIN=/nonexistent/claude npx tsx src/main.ts` + 一次 `POST /api/tasks`，**未调用真实 claude**）：
     **修复前** → 服务进程 **exit 1**（输出 `throw er; // Unhandled 'error' event` → `Error: spawn /nonexistent/claude ENOENT`，健康检查 curl exit 7）；
     **修复后** → 服务存活（`/api/health` 仍 `{"ok":true}`），任务正常落 `status=failed`，
     `lastError` = 「CLI 退出码 -1：进程启动失败：spawn /nonexistent/claude ENOENT」，`log_ref` 指向真实存在的 jsonl。验证用的临时库/日志已清理。
     另记一个**测试方法论陷阱**（已写进用例注释）：直接在 vitest 测试函数体里 `for await` 迭代**无法**复现该崩溃 ——
     vitest 已处于微任务上下文，yield 的续跑微任务先于 nextTick 队列执行、监听器「恰好」已挂上（实测挪回旧位置仍 `1 passed`）；
     用例必须用 `setImmediate` 跳出微任务上下文，与生产路径（HTTP 处理器在宏任务的同步段里启动 `runTask`）同形。
  2. **文档 4 项全部同步**：① `记录.md` 中「`WS /ws` 广播 `{type:'event'}`」的陈旧表述改为如实描述
     （Phase 1 只推 `task_state` / `task_error`，`event` 为 Phase 2 预留的死代码）；② `记录.md` 的 Task 15 清单项改为最终事实
     （PASS + 3/3 节点 / 3/3 artifact / `self_test_result=passed` / 461→7 / $0.5876586），**首轮 FAIL 历史与根因一并保留**；
     ③ spec §11.3 补「Phase 1 实现修正」块（六前缀 + `acceptEdits`、shell 前缀 ≡ 任意执行、git/npm 授权面过宽、
     只读角色非 airtight、「安全由 CLI 强制」只算部分成立）；④ spec §7.2 / §15 标注 `owns` 零强制（并登记进「未完成」）。
     ⑤ 计划文档 Task 10 Step 4 的参考实现有**同一个 spawn 缺陷**，已一并修正（否则将来重抄会复现）。
  - 验证：全量 `npx vitest run` = **133/133 通过**（19 文件，新增 1 条 spawn 回归用例）、`npx tsc --noEmit` exit 0。
  - 报告：`.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-final-fix-report.md`。

### 最近更新（2026-09-19 端到端重跑验证）

- **权限修复已由真实端到端复跑证实生效：`bash tests/e2e/smoke.sh` 一次跑通，输出 `PASS：任务完成`**。
  全程在 `mktemp -d` 出来的独立临时 git 仓库 `/tmp/agentflow-e2e-target-r7zam3` 里跑（未在 AgentFlow 仓库根目录跑），
  `chmod +x tests/e2e/smoke.sh` 后执行，**只跑了这一次**（用户只批准一次）。
  - **结果**：`status=completed`、`completedNodeIds=["pm_analyze","dev_implement","qa_verify"]`、
    3 条 `transfer.decided`（`decidedBy` 全为 `rule`）、**3 个 artifact**（`requirement` / `code_diff` / `test_report`，均 `status: ok`）、
    **3/3 `log_ref` 全部 `OK`**、事件总数 **23**（`task.created` → `task.completed`）、全程 **213.4s（3 分 33 秒）**。
  - **节点耗时与花费（事件库 `created_at` 与 `node.usage_recorded` 实读）**：
    `pm_analyze` **32.8s / $0.054723**（in 6627, out 2118）、`dev_implement` **70.5s / $0.2042082**（in 5059, out 5611）、
    `qa_verify` **110.0s / $0.3287274**（in 21865, out 6912）。**实测总花费 $0.5876586**。
  - **关键指标 —— 权限修复是否生效：是**。`dev_implement` 的 `code_diff` artifact 真实产出
    **`"self_test_result":"passed"`**（上一轮只能是 `not_run`）；日志里 `requires approval` 只有 **7 次**
    （dev 3 / qa 4 / pm 0），**对比上一轮 461 次下降 98.5%**。真实执行的命令（从 `tool_use` 提取）：
    dev 12 次 Bash（含 `chmod +x scripts/hello.sh`、`bash scripts/hello.sh`、`bash -n ...`、`od -c` 字节校验）、
    qa 17 次 Bash（含自建 `tests/test_hello_smoke.sh` 并 `bash tests/test_hello_smoke.sh` **跑了 3 轮**，
    其中一轮是**故意的变异验证**——把脚本改成 `hello world` 确认测试会红）、pm 0 次 Bash。
  - **成本/耗时全部回落到正常量级**：dev 单节点由上轮 516.5s/$1.1291（第 1 次）与 877.7s/$2.2017（第 2 次）
    降到 **70.5s/$0.2042**；qa 由上轮「从未成功」变为 **110.0s/$0.3287 成功**。
  - **残留的 7 次拒绝仍属同一类语义**：六类前缀之外仍会被拒 —— `./scripts/hello.sh`（直接按 shebang 执行，不经 `bash`）
    与「复合命令中的非白名单片段」（如 `; wc -c ...`、`; command -v shellcheck`）。agent 都能迅速改道，
    未再陷入上一轮的长时间重试循环。
  - **隔离与仓库干净度**：临时仓库 `git status --short` 为 `?? README.md` / `?? data/` / `?? logs/` / `?? scripts/` / `?? tests/`
    （agent 真实产物：`scripts/hello.sh` 可执行、`README.md` 追加「冒烟脚本」小节、qa 自建测试脚本）；
    **AgentFlow 仓库 `git status --short` 为空、`git diff --name-only` 计数为 0**（除本任务待提交的 `记录.md`）。
  - **全量单测无回归**：`npx vitest run` = **132/132 通过**（18 文件）。
  - 复跑产物留档：完整 stdout `/tmp/agentflow-e2e-smoke-output-3.txt`、目标仓库 `/tmp/agentflow-e2e-target-r7zam3`
    （含事件库 `data/e2e.sqlite` 与 3 个 `logs/runs/*.jsonl`），均未入库。

### 最近更新（2026-09-19 权限评审补洞）

- **评审 3 项 Important 已补齐（纯注释 + 断言，未改任何授权参数）**：核心判断（只读边界未被放宽、未引入
  `--dangerously-skip-permissions`）评审已确认正确，本轮只补标注与断言缺口：
  1. **标注 shell 前缀 ≡ 任意命令执行**：`src/runner/claude-code-runner.ts:173-189` 删掉原句
     「在不放弃 CLI 权限强制的前提下」（该表述高估了实际强度），显式写明 `bash -c "<任意命令>"` 落在
     `Bash(bash:*)` 内，故 `git push --force` / `rm -rf` / `curl … | sh` 均可绕过前缀限制 ——
     **「精准预授权」在能力层面已退化为「全量放行」，前缀白名单实际只约束不包 shell 的调用**。
  2. **`git` / `npm` 注释改为如实描述实际授权面**：`Bash(git:*)` 涵盖**全部** git 子命令（含 push / reset --hard /
     clean -fdx），`Bash(npm:*)` 涵盖 publish 与任意 install 生命周期脚本（`node` 一并如实描述）；
     并写明「Phase 1 目标仓库是临时目录尚可控，真实项目使用前需按需收窄（如 `Bash(npm test:*)` / `Bash(npm run:*)`）」。
     **六类前缀本身一字未动**（收窄属 Phase 2 决策）。
  3. **只读边界断言补洞**：原两条断言都是**子串**判断，而 `--dangerously-skip-permissions` 两个子串都不含 →
     用户明确否决的那一项正好从洞里漏过去。`claude-code-runner.args.test.ts:54-84` 补两条：
     ① 只读 args `not.toContain('dangerously-skip-permissions')`；② 只读 args **整串精确断言**（`toEqual`）。
  - **变异验证（证明洞已补上的唯一方式）**：临时给只读分支加 `--dangerously-skip-permissions` →
    **新断言变红（2 failed | 9 passed）**；同时打印证实**改前两条断言对这次变异全部放行**
    （`含 --allowed-tools = false`、`含 Bash 子串 = false`）→ 洞是实打实的，不是理论推断。已还原。
  - **计划文档**：`docs/superpowers/plans/2026-09-18-agentflow-phase0-phase1.md:3396-3426` 按 spec §11.2 的
    「实测修正」写法补入 Task 10 段（3 条已知局限/风险）。
  - **验证**：全量 **132/132 通过**（18 文件，改前 131，新增 1 条精确断言用例）、`npx tsc --noEmit` exit 0；
    本轮零探针、**未调用真实 claude**、无新依赖、未触碰全局环境。
    报告：`.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-perm-audit-report.md`。

### 最近更新（2026-09-19 权限修复）

- **headless 下 agent 无法执行命令的缺陷已修复（精准预授权）**。先花 **$0.374775** 跑了 6 组真实探针（全部在
  `/tmp/agentflow-perm-probe`，未在本仓库跑），再用事实改代码、用测试锁死：
  - **探针结论**：`--permission-mode acceptEdits` + `--tools=…,Bash`（无预授权）下 `sh -c 'echo x'` 返回
    `This command requires approval`（对照探针 A，`permission_denials` 有 1 条）；
    追加 `--allowed-tools` 后 `sh`/`bash`/`chmod`/`git`/`node`/`npm` **全部真正执行成功、`permission_denials` 为空**
    （探针 B~E，含 `git checkout -b` 真的建出分支、`chmod +x` 在文件系统上可复核）。
    `--tools`（限定可用工具集）与 `--allowed-tools`（集合内预授权命令）**语义不同但不冲突，应同时给出**。
  - **代码**：`src/runner/claude-code-runner.ts` 的 `buildArgs` —— 可写角色在
    `--permission-mode acceptEdits --tools=Read,Edit,Write,Grep,Glob,Bash` 之后追加
    `--allowed-tools 'Bash(sh:*),Bash(bash:*),Bash(chmod:*),Bash(git:*),Bash(node:*),Bash(npm:*)'`；
    **只读角色一字未改**（仍 `default` + `Read,Grep,Glob`，不含任何 Bash 预授权）。
    **未使用 `--dangerously-skip-permissions`**（用户已否决，且测试有断言把守）。
  - **真实 runner 路径已验证**：走改后的 `createClaudeCodeRunner` 跑一次最小请求，agent 真的执行了
    `sh -c 'echo runner-ok'` 并把输出填进结构化产物（artifact `{"note":"runner-ok"}`、exit 0、$0.0333）。
  - **测试**：`claude-code-runner.args.test.ts` 新增 3 条（只读角色不含 Bash 预授权 = 安全边界；
    预授权值精确断言；`--tools`/`--allowed-tools` 并用且不含 `dangerously-skip-permissions`），
    原有断言全部保留 → **131/131 通过**，`npx tsc --noEmit` exit 0。
  - **`tests/e2e/smoke.sh`**：`sed` 贪婪导致「取到 artifacts[].status=`ok`、早退永不触发」的真实缺陷已改用
    `node` 解析任务**顶层** `status`（实测该表达式对含 `artifacts[].status` 的样本返回 `active`）；
    轮询窗口 180×5s → **480×5s（40 分钟）**，每轮打印已等待时长；`bash -n` 语法检查通过。
  - **未验证**：下一轮真实端到端的重试次数与单节点成本是否回落（`qa_verify` 的 `node.succeeded` 仍待验证）；
    探针每组 n=1，不代表稳定行为。报告：`.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-perm-fix-report.md`。

### 最近更新（2026-09-19）

- **Task 15（端到端冒烟）已执行，实测结果 FAIL**：新增 `tests/e2e/README.md`、`tests/e2e/smoke.sh`（与简报代码块逐字一致，
  仅差结尾换行），并在 `mktemp -d` 出来的独立临时 git 仓库里**真实跑了 2 次**（未在 AgentFlow 仓库根目录跑）。
  两次结果一致：3 个节点都进入 `node.started`，但只有 `pm_analyze`、`dev_implement` 成功（2/3），
  `qa_verify` 被 `smoke.sh` 15 分钟轮询窗口到点后的 trap 中断，最终状态 `active` 而非 `completed` → 脚本判定 `FAIL`。
  **实测总花费 $3.39695**（$1.1678724 + $2.2290776，均为 `budgetUsedUsd` 实读）。
  根因**不是**内核/状态机缺陷，而是 **runner 在 headless 下 Bash 工具被自动拒绝**
  （`--permission-mode acceptEdits` 只放行文件编辑，`chmod`/`bash x.sh`/`git checkout -b` 全返回 `This command requires approval`），
  与 dev/qa prompt「必须真实运行自测/测试」直接冲突 → agent 陷入重试循环（dev 单节点 8.6~14.6 分钟、
  $1.129~$2.202，超预期 8~44 倍）；叠加 `smoke.sh` 轮询的 `sed` 贪婪匹配导致早退永不触发，
  真实耗时必然超过 15 分钟窗口。**已真实验证到**：隔离生效（AgentFlow 仓库 0 个已跟踪文件被改）、
  `log_ref` 两次全部 `OK`、3 条 `transfer.decided`（均 `rule`）、`requirement`/`code_diff` 两 artifact 载荷合规；
  **未验证到**：`qa_verify` 的 `node.succeeded` 与 `test_report` artifact。全量单测仍 **128/128**、`tsc --noEmit` exit 0。
  详见下方「Phase 1 实测结果」与 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-15-report.md`。

### 最近更新（2026-09-18）

- **Task 14 完成（极简观测页面）**：新建 `web/` **独立前端包**（自带 `package.json`，与根 Node 后端分离），
  8 个文件与简报逐字一致：`package.json` / `vite.config.ts` / `tsconfig.json` / `index.html` /
  `src/{main.tsx,App.tsx,api.ts,styles.css}`。页面 = 左侧「创建表单 + 任务列表」+ 右侧「任务详情节点流转时间线」
  （节点按 `completedNodeIds` 排序、artifact 卡片、transfer 进入理由、`budgetUsedUsd` 与并行度）。
  依赖安装走国内镜像 `npm install --registry=https://registry.npmmirror.com`（67 包，4s）——
  **仅写入 `web/` 自己的 `package.json` / `package-lock.json`，根目录两份文件一字未动，无任何全局安装**。
  验证：`npx tsc --noEmit` **退出码 0、无输出**；`npx vite build` **成功**（vite v5.4.21，32 modules，
  `dist/index.html 0.40kB` + `assets/index-*.css 2.33kB` + `assets/index-*.js 145.81kB`，`web/dist/` 合计 152K）。
  `git check-ignore -v web/dist` → `.gitignore:6:dist/`（无前导斜杠，对 `web/dist/` 同样生效，**未改根 `.gitignore`**）。
  **能力边界已按 Task 13 裁决实现，未误用**：Phase 1 的 WS 只推 `task_state`（任务终态快照）与 `task_error`，
  故运行中进度**靠 2 秒轮询 `GET /api/tasks/:taskId`**（`App.tsx` 的 `setInterval(..., 2000)` 原样保留、未被"优化"掉），
  WS 仅作兜底刷新。已只读核对后端：`server.ts` 端点与广播通道、`projector.ts` 的 `TaskState`/`Artifact`/`TransferRecord`
  字段、`env.ts` 默认 `127.0.0.1:8787`（与 `vite.config.ts` 代理目标一致）——前端 DTO 与真实返回逐一对应。
  未因 `React.FormEvent` 踩坑（保持 `import { ..., type FormEvent } from 'react'`）。
  已知取舍（未自行扩展）：`task_error` 静默忽略、创建后详情首帧可能瞬时 404（下轮轮询自愈）、未渲染 artifact `payload`。
  提交：`604b8a3`（仅 `web/`，含自动生成的 `package-lock.json` 以满足依赖锁定）。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-14-report.md`。
- **Task 13 完成（HTTP + WebSocket 服务与进程入口）**：新增 `src/server/server.ts`（Fastify + `@fastify/websocket`，
  `createServer({kernel,host,port})` 返回 `{app, close}`）、`src/server/server.test.ts`（7 条）、`src/main.ts`（进程入口，
  **首次把配置加载 / 事件库 / Runner / 内核 / 服务全部接线**）。端点齐备：`POST /api/tasks`（zod 校验，201 返回 `{taskId}`，
  **异步**触发 `runTask` 不阻塞响应）、`GET /api/tasks`、`GET /api/tasks/:taskId`、`GET /api/tasks/:taskId/events`、
  `GET /api/health`、`WS /ws`（**Phase 1 实际只推 `{type:'task_state',state}` 与 `{type:'task_error',message}`**；
  `{type:'event',event}` 在 `src/server/server.ts` 里**没有任何广播点**（内核未暴露事件订阅接口），
  属 Phase 2 预留的死代码，见 spec §13.2 的注解。前端运行中进度靠 2 秒轮询 `GET /api/tasks/:taskId` 兜住）。
  TDD：RED = `Failed to load url ./server.js`（模块不存在，非语法错误）→ GREEN = 7/7（首跑即绿）；
  全量 **128/128**（18 文件）、`tsc --noEmit` exit 0、输出干净无 warning。
  **接线不变式已实测**（本次唯一具名风险）：内核 `logDir = resolve(env.logDir)` = `<repo>/logs`，
  runner `logDir = resolve(env.logDir,'runs')` = `<repo>/logs/runs`；内核 `log_ref = join(logDir,'runs',runId+'.jsonl')`
  = `<repo>/logs/runs/<runId>.jsonl` **与 runner 实际落盘路径逐字相等**（用 `tsx --eval` 打印两串比对，`INVARIANT HOLDS = true`）。
  两者均为**绝对路径**，故不存在「同一文件两种字符串形态」。
  **数据库路径**用 `resolve(env.dbPath)`（默认 `./data/agentflow.sqlite`，扩展名 `.sqlite` 在 `.gitignore` 覆盖内），未改成 `.db`。
  **只绑 `127.0.0.1`**：host/port 一律取自 `loadEnv()`（默认 `127.0.0.1:8787`），未硬编码。
  **手工冒烟**：`npx tsx src/main.ts` → `/api/health` 返回 `{"ok":true}`、POST 返回 `{"taskId":"task_e5cb8206ec9442c6b355"}`；
  另用 Node 全局 `WebSocket` 单独验证 `ws://127.0.0.1:8787/ws` 握手成功（`WS_OPEN`/`WS_CLOSE`，退出码 0）。冒烟后无残留进程（`claude`/`tsx` 均无）。
  **对简报的 1 处必要补充（仅测试夹具，断言原文一字未改）**：`role()` 补 `maxRetries: 1` —— Task 11 已把 `maxRetries`
  变成 `RoleDef` 必填字段（与 Task 12 同因），不补则 `tsc` 报 TS2739。
  **已遵守的既定取舍（未改）**：① 服务层任务登记只在内存（`knownTaskIds`/`taskSummaries`），只有 POST 会登记，
  故 HTTP 读接口对「直接由内核 `startTask` 建出的任务」返回 404（Phase 2 改为从事件库聚合）；② POST 异步触发 `runTask`。
  测试全部注入 `FakeRunner`，**未真的调用 claude**；`afterEach` 保留简报的 50ms 等待后再关服务与事件库（先关服务再 `store.close()`）。
  未新增依赖、未改前序文件、未触碰全局环境、未创建简报之外的文件。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-13-report.md`。

- **Task 12 完成（调度器与内核主循环）**：新增 `src/kernel/scheduler.ts`（`git worktree` 包装 + 非 git 退化路径）、
  `src/kernel/kernel.ts`（主循环 `project → decideNext → 装配 prompt → runner → 追加事件`）、`src/kernel/kernel.test.ts`（14 条）。
  两份源码经脚本比对与简报代码块**逐字一致**；未改动任何前序文件、无新依赖。
  TDD：RED = `Failed to load url ./kernel.js`（模块不存在，非语法错）→ GREEN = 14/14（首跑即绿）；
  全量 **121/121**、`tsc --noEmit` exit 0。
  **对简报的 2 处必要偏离（都在测试夹具里，13 条断言的原文一字未改）**：
  ① 角色夹具补 `maxRetries: 1` —— Task 11 把 `maxRetries` 变成 `RoleDef` 必填字段，不补则 `tsc` 报 TS2739；
  ② 日志目录夹具改为 `<mkdtemp>/logs` —— 简报的 `mkdtempSync(join(tmpdir(),'agentflow-logs-'))` 得到的末级目录名是
     `agentflow-logs-XXXXXX`（随机后缀在 `logs` 之后），实测其下 `logs/runs/...` 子串**不存在**，断言
     `match(/logs\/runs\/[^"]+\.jsonl/g)` 必然为 null（已用一行 node 复现）。改夹具后与生产形状一致：Task 13 给 kernel 传
     `resolve(env.logDir)`、给 runner 传 `resolve(env.logDir,'runs')`，即 runner 写的文件正是 kernel `log_ref` 指向的文件。
  **自补 1 条用例**：`rc=0 且零 artifact → 节点失败`（简报 13 条无此覆盖，而 `hasArtifact` 是本任务新增的必守判据）。
  三条硬约束都做了变异验证：① `if (!hasArtifact)` 置恒假 → **仅**该条变红（1 failed | 13 passed）；② `parseArtifactPayload`
  换成直接赋值 → 恰好 2 条载荷用例变红；③ 把日志正文写进事件 → 「日志不进事件库」用例变红。
  **诚实发现**：当前组合下「零 artifact」即便删掉 `hasArtifact` 判据也会失败（`parseArtifactPayload(undefined)` 抛 zod 错），
  该判据的可观测差异是**错误归因**（「未产出结构化结果」vs「载荷校验失败」），而非「会不会被误判成成功」——已在报告写明。
  **残留缺口**：`transfer.decided` 的 `reason` 有写入但无直接断言（`decided_by` 由 test 4 覆盖）；
  `scheduler.ts` 的 worktree 分支按简报说明仍**未覆盖**（Phase 1 全节点 `isolate: false`，属 Phase 2 骨架）。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-12-report.md`。

- **裁决反转的文档整顿与证据补档（本轮，未改任何代码逻辑）**：评审裁定「有条件接受」，本轮修掉 5 处 Important + 1 处 Minor。
  1. `spikes/cli-probe/README.md:105`：原「运行时从未观测到 `structured_output`…属静态验证」改写为
     **当前结论（补跑实测已确认）+「原记录（凭据耗尽期，情境：请求全部 403）」**，并标明后者已被取代。
  2. 计划文档 `:3698` 参考实现里 `useJsonSchema` 的兜底默认值由 `false` 改为 **`true`**（与同页 `:3590`/`:3597`、
     真实实现一致）；同步把同段注释里「result 退化为散文」改为「result 形状不确定」。
     全局检索 `useJsonSchema`（计划文档仅剩 `= true` 与合法用例名）、`默认不传`（计划文档 0 处）。
  3. README `:317`/`:355` 的「带 `--json-schema` 时 `result` 是散文」被**其自身归档的 fixture 证伪**
     （`claude-stream-structured-sample.jsonl` 的 `result` 实为 ```` ```json ```` 代码块）→ 改为
     「`result` 形状**不确定**（散文/代码块都出现过），**只有 `structured_output` 可靠**」；
     连带订正 `记录.md` 里「样本必须是散文 result」的守卫描述（实际只校验 `is_error`/`typeof result`/`typeof structured_output`）
     与 `claude-code-runner.fixture.test.ts` 的同类注释。
  4. **补档**：从 `/tmp/agentflow-e2e-logs/plain/real-plain.jsonl` 找到「不带 schema」那条 `result` 行，
     逐字归档为 `tests/fixtures/claude-stream-plain-sample.jsonl`（`is_error:false`、`result` 为 ```` ```json ```` 代码块、
     无 `structured_output`、`total_cost_usd`=$0.1159 与实测表一致）——该侧从「不可复核」变为可复核。
     README 结论补齐 **n=1 与随机性**（两条路径各只跑 1 次，「模型是否用代码块包裹」本质是随机行为）。
  5. README 把「`rc=0` + `is_error:false` 但零 artifact」标为**已知开放项**：runner 层无「成功即须有 artifact」断言，
     该判据由内核层 `hasArtifact` 承担、Phase 1 在 **Task 12** 落地；并订正 `src/runner/types.ts` 中与实现不符的注释。
  - **Minor**：`src/runner/claude-code-runner.ts:56` 判 `structured_output` 为对象时补 `!Array.isArray(...)`（数组不再误通过）。
  - 本轮**仅改文档/注释 + 补 1 个 fixture + 1 处判定收紧**，未改 `buildArgs` 默认值、`parseStreamLine` 通道优先级、
    `killTree`/`detached`/超时/退出钩子；未调用真实 CLI、未触碰全局环境、无新依赖。

- **Task 11 完成（配置加载：角色与工作流）**：新增 `src/config/schema.ts`（zod 3 文件 schema：RoleFile / WorkflowNode / WorkflowEdge / WorkflowFile）、
  `src/config/loader.ts`（`loadRole` / `loadAllRoles` / `loadWorkflow` / `readPromptFile`）、
  `src/config/loader.test.ts`（8 条）、`src/config/real-config.test.ts`（2 条），以及 `config/prompts/{pm,backend_dev,qa_engineer}.md`、
  `config/roles/{pm,backend_dev,qa_engineer}.yaml`、`config/workflows/simple_dev.yaml`（共 11 个文件，代码与配置逐字照抄 Task 11 简报）。
  角色 YAML 校验通过后把 `system_prompt_ref` 指向的 markdown **内联**进 `RoleDef.systemPrompt`（`.trim()`），
  加载期即暴露缺文件/结构非法问题；所有错误信息都带**绝对文件路径 + 具体原因**（如
  `找不到角色文件：<dir>/config/roles/nope.yaml`、`角色配置校验失败：<path> —— <zod message>`），不抛裸 ZodError。
  `loadWorkflow` 除 schema 校验外还断言 `start` 指向真实节点、每条边的 `from`/`to` 都存在（不存在则报出节点名）。
  **范围约束已守住**：`simple_dev.yaml` 三个节点 `isolate` **全为 false**（Phase 1 无合并能力，worktree 隔离属 Phase 2），
  已用 `tsx` 打印实测确认；配置文件不含任何路径/密钥，未改 `.gitignore`（`config/` 为声明式配置，应入库）。
  TDD：RED = `npx vitest run src/config/loader.test.ts` → `Failed to load url ./loader.js`（模块不存在，非语法错误）
  → GREEN = 8/8，`real-config.test.ts` 2/2，**全量 105/105，`tsc --noEmit` exit 0，输出干净无 warning**。
  变异验证：临时删掉 `loadWorkflow` 的 start/边存在性校验 → 恰好那 2 条用例变红（`2 failed | 6 passed`），
  证明断言非空跑，已还原（`git status` 干净）。**vitest CWD 实测为仓库根**（`RUN v2.1.9 /Users/40kuai/Documents/多agent开发流程`，
  且 `real-config.test.ts` 用相对路径 `./config` 直接通过），故无需改绝对路径。无新依赖、未触碰全局环境。
  ⚠️ **提交异常（需控制者知悉）**：本任务文件被一个**并发进程**的提交 `80d1726`（message 为
  `feat: --json-schema 改为默认开启并解析 structured_output`，属 Task 10 gate 收尾）**一并卷入**——
  我在 `git add src/config config` 之后、`git commit` 之前，该并发提交先行落地并带走了我已暂存的文件，
  导致我的 `git commit` 报「nothing to commit」。**我的 11 个文件内容已在 `80d1726` 中且完整无误**
  （已用 `git show HEAD:<path>` 逐个核对行数），但该 commit 不是我的独立提交、message 也不描述本任务。
  未做任何历史重写（避免破坏性操作），仅在此如实记录。详见
  `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-11-report.md`。

- **Task 11 评审后修复（1 Important + 2 Minor，已修，commit `10f3df7`）**：
  `fix: 配置加载带出 max_retries 并补 isolate 硬约束护栏`（5 files，+41/-2）。
  ① **Important（`max_retries` 被静默丢弃）**：`RoleDef` 增加 `maxRetries: number`
  （`src/shared/domain.ts:25`，位于 `maxWallTimeMs` 旁，camelCase），`loadRole` 映射补
  `maxRetries: file.max_retries`（`src/config/loader.ts:46`）。因 `RoleDef` 是必填字段，
  同步给唯一一处对象字面量 `src/kernel/context-assembler.test.ts:16` 补 `maxRetries: 2`（否则 `tsc` 报错）。
  新增护栏用例（`src/config/loader.test.ts:60-80`）用**非默认值 1**（schema 默认为 2）断言
  `loadRole(dir,'qa').maxRetries === 1`，可区分「读到真实值」与「用了默认值」。
  **变异验证**：临时删掉 `maxRetries: file.max_retries` 那行 → 仅该用例变红
  （`expected undefined to be 1`，`1 failed | 8 passed`），已还原。
  ② **Minor 1（`isolate=false` 无护栏）**：`src/config/real-config.test.ts:24-33` 新增用例，
  断言 `simple_dev` 所有节点 `isolate` 均为 `false`，注释写明理由（Phase 1 无合并能力，隔离即丢代码、断闭环）。
  ③ **Minor 2（`readYaml` 裸 catch 吞 errno）**：`src/config/loader.ts:11-14` 读取失败改为
  `读取${label}文件失败：${path} —— ${(error as Error).message}`，保留 EACCES/EISDIR 底层信息；
  **顺带订正**：原「找不到…」对权限/目录错误是误诊，故措辞由「找不到」改为「读取…失败」。
  验证：`npx vitest run` = **107/107 通过**（原 105 + 新 2 条，16 文件），`npx tsc --noEmit` exit 0；
  `src/config` 单跑 16/16。**未动** YAML 值、`src/runner/**`、`tests/fixtures/**`、`artifacts.ts`/`events.ts`，
  未改其他已记台账的 Minor。另注：编辑过程中发现 `loader.ts` 的 `readYaml` 改动曾一次被并发进程回退，
  已复核重做并确认落盘（同一并发风险与上条 §6 提交异常同源）。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-11-report.md`（同文件追加修复报告）。

- **Task 10 gate 收尾（凭据恢复后补跑，结论与计划原裁决相反）**：
  1. **凭据已通**：`claude -p "reply with exactly: OK" --output-format json` → `is_error:false`、cost `$0.071142`。
  2. **gate 实验结果**：带 `--json-schema` 的真实调用 **rc=0、7.0s 正常退出**，`result.structured_output`
     为符合 schema 的对象（运行时首次观测到该字段，此前只有静态验证）。原先「永不退出」只发生在
     **请求持续失败**时（stop hook 反复注入 `You MUST call the StructuredOutput tool`），凭据正常时不复现，
     但 wall-clock 超时 + 按进程组 kill 必须保留。
  3. **真实 Runner 端到端双路径对比**（`createClaudeCodeRunner` + tsx，同一 prompt 各跑一次）：

     | 路径 | 耗时 | 产物 | 成本 |
     | --- | --- | --- | --- |
     | 不传 `--json-schema` | 29.3s | ❌ **零 artifact**（模型把 JSON 包进 ```json 代码块，`JSON.parse` 失败） | $0.1159 |
     | 传 `--json-schema` | 15.9s | ✅ 4 字段齐全（CLI 校验过） | $0.0507 |

     即计划里「默认不传」这条裁决**被新证据推翻**（不传时产物不可靠）。
  4. **改动**（3 个源文件 + 1 个新 fixture）：`buildArgs` 第二参数默认值 `false → true`（`options.useJsonSchema ?? true`）；
     `parseStreamLine` 增加 **`structured_output` 优先**分支（只读 `result` 会让开启 schema 的调用静默零产物），
     `result` 内 JSON 字符串作为兜底；`claude-code-runner.args.test.ts` 断言随规则反转（新增「无 outputSchema 不传」
     与「显式关闭不传」两条）；新增真实成功样本 fixture `tests/fixtures/claude-stream-structured-sample.jsonl`
     （逐字取自本次真实调用；其 `result` 实测是 ```` ```json ```` 代码块，**不是散文**）与契约回归用例
     （断言 artifact 与样本自身 `structured_output` 逐字相等）。**订正**：该用例的前提守卫实际只校验
     `is_error===false`、`typeof result === 'string'`、`typeof structured_output === 'object'`，
     **并不校验「result 是散文」**（原文「样本必须是散文 result」的表述与断言不符，已更正）。
  5. **验证**：新用例先 RED（2 failed）后 GREEN；全量 **95/95**、`tsc --noEmit` exit 0；无新依赖、未改 fixture 之外既有文件。
  6. **文档同步**：`spikes/cli-probe/README.md`（第 3 问改为已验证、gate 改为「已解除」并附双路径实测表、
     复现方式与证据索引补齐）、计划文档（Task 10 假设表 + 简报代码块 + Step 5 加「2026-09-18 修订」）。
  7. **另**：工作区遗留的 Runner 退出钩子改动（孤儿进程组兜底清理 + 超时护栏确定性加固）已单独提交
     为 `f467707`，其配套记录为 `548cc5f`。

- **Task 10 评审后修复（2 处 Important，均已修）**：① **超时护栏的确定性** —— 评审给的 Node 假 bin
  （显式 `spawn` + `stdio:'inherit'`）落地后**仍会假绿 1/5**（把超时路径 `killTree` 退回 `child.kill('SIGKILL')`
  时第 1 次 305ms 就通过）。插桩定位真因：**macOS 对「新建脚本文件首次 `execve`」有一次校验成本 272~988ms**
  （A/B 实测：显式 `node <script>` 20ms；直接 execve 新脚本首跑 272/988ms、次跑 ~22ms；`#!/bin/sh` 首跑同样 459ms），
  故 300ms 窗口内假 bin 常常还没启动、孙进程不存在 → `close` 立即触发。修法是**保留评审的假 bin 形态，
  另加一步 `execFileSync(bin, ['--warmup'])` 预热**（`claude-code-runner.timeout.test.ts:27-32`），把该成本移出计时窗口；
  并补「进程组已消失」断言（`:65-73`，`process.kill(-pid,0)` 抛错）。**变异验证 5/5 稳定 RED**（各 11~12s，
  均为 `close` 永不触发的 10s 超时），对照预热前 4/5 红。② **`detached: true` 的孤儿风险** —— 在
  `claude-code-runner.ts:88-115` 加模块级 `activeGroups` + 一次性 `process.once('exit')` 钩子（只注册一次，
  不注册 SIGINT/SIGTERM 以免与 Task 13 的 `main.ts` 冲突），`:190` 登记、`close`/`error` 两处 `:251`/`:261` 注销。
  端到端验证（临时脚本，已删）：2 个并发 run → `EXIT_LISTENERS` 只 +1；`process.exit(0)` 后两个进程组均消失、无孤儿。
  **边界如实记录**：变异运行时 vitest 强杀挂起 worker，`exit` 钩子来不及执行 → 5 个孤儿 `sleep 60`（已 `kill -9` 清理），
  这正是 SIGKILL 不可捕获的设计边界。**踩坑提醒**：同一文件的多处编辑放在同一批里发出会被「后写覆盖前写」
  （本轮 `registerGroup(...)` 调用点与 `close` 处的 `unregisterGroup(...)` 一度被静默丢弃），必须逐条串行编辑 + 检索复核。
  修复后：超时用例 585~604ms、连跑 5 次全绿；全量 **90/90**（连跑 3 次一致）；`tsc --noEmit` exit 0；无残留进程。
  无新依赖、未改 fixture、未触碰全局环境、未调用真实 CLI。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-10-report.md`（同文件追加「第二轮」）。

- **Task 10 完成（Claude Code Runner 适配器）**：新增 `src/runner/claude-code-runner.ts` 与 4 个测试文件 +
  `tests/fixtures/claude-stream-sample.jsonl`（6 files）。`parseStreamLine` / `buildArgs` 为纯函数可脱离真实 CLI 单测，
  `createClaudeCodeRunner` 负责 spawn 并把 stdout 逐行喂给解析器。逐字落实 Task 2 的实测结论：**默认不传
  `--json-schema`**（实测会让 CLI 永不退出）、**失败只看 `is_error` 不看 `subtype`**、解析层**无字段白名单**、
  `-p`+stream-json **必带 `--verbose`**、`result` 对「JSON 字符串/对象」两形态容错、**硬性 wall-clock 超时并 kill**。
  fixture 逐字取自探针 README 内联原始 JSONL 的 `result` 行（认证失败样本，`is_error:true`），契约回归的期望值
  从样本自身 `is_error` 推导。**TDD**：RED = `Failed to load url ./claude-code-runner.js`（模块不存在，非语法错误）
  → GREEN 首跑 1 failed | 5 passed（见下「简报矛盾」）→ 修正后 6/6；全量 **90/90**，`tsc --noEmit` exit 0。
  **两处对简报的必要修正**：① **实现**——超时时原本只 `child.kill('SIGKILL')` 直接子进程，实测会真实挂起：
  `#!/bin/sh` 会 **fork** `sleep 60`，孙进程继承 stdout 管道写端，只杀 `sh` 会让 Node 的 `close` 事件**永不触发**，
  异步生成器卡死（修复前 3 次复跑 2 次挂满 10s；这正是 Task 2「永不退出」路径的同类形态）。已改为
  `spawn(..., { detached: true })` + 新增 `killTree()` 按**进程组**杀（`process.kill(-pid)`，回退直接杀），
  超时与 `cancel` 共用；修复后超时用例 **306ms** 结束、连跑 6 次稳定、无残留 `sleep` 进程。
  ② **测试**——简报 Step 1 的 usage 断言（`toEqual([usage])`）与简报自身的实现/相邻用例/Step 7 契约
  （成功样本须产出 artifact）**自相矛盾**，按最小改动改为 `toContainEqual({usage})`（保留原意，artifact 由专用用例覆盖）。
  **额外发现并补 1 条判别用例**：变异验证（把 `isError` 改成看 `subtype`）显示**简报原有 20 条用例全部照常通过**
  ——真实归档样本虽是最危险的 `subtype:"success"`+`is_error:true` 形状，但其 `result` 非 JSON，两种判据输出巧合一致，
  探针头号陷阱竟零覆盖。新增用例用「`subtype:"success"` + `is_error:true` + `result` 为合法 JSON」钉固该规则，
  变异下**仅此一条变红**（`1 failed | 6 passed`），确认为唯一把守。无新依赖、未改既有文件、未触碰全局环境。
  ⚠️ **未做**：`result.structured_output` 路径按 gate **不纳入解析**；额度恢复后需补跑一次带 `--json-schema`
  的真实调用（加超时+kill）才能决定。详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-10-report.md`。

- **Task 9 评审后修复（2 处 Important，已修）**：① 回退 `AssembledPrompt` 的 `worktreePath` 字段
  （`src/kernel/context-assembler.ts:5-11` 类型、`:126-132` 返回值），恢复简报声明的 5 字段形状；测试里那条
  恒真断言 `expect(r.worktreePath).toBe('/tmp/ws')` 改为 `expect(r.prompt).toContain('/tmp/ws')`
  （`src/kernel/context-assembler.test.ts:82`），首次真正覆盖「工作区路径写进 prompt」这一此前零覆盖的行为。
  ② 删除 `renderArtifactBlock` 的 `includeSummary` 参数与 `else` 分支（`:29-41`），调用点不再传参（`:65`）——
  净减 6 行、零行为变化。**同时更正原报告一处不实描述**：被丢弃产物的 prompt 块**不是**换成「已因上下文长度
  限制省略」文案（该文案随死分支删除，从未出现在任何 prompt 里），而是**整块从「输入材料」节移除、仅 refs 进
  「被省略的输入材料」节**。**变异验证**：临时恢复 `else` 分支（含原文案）并让调用点传 `true`，重跑
  `npx vitest run src/kernel/context-assembler.test.ts` → **7/7 仍全绿、无任何测试变红**，实证该分支不可达；
  已还原。修复后单文件 **7/7**、全量 **75/75**、`npx tsc --noEmit` 退出码 0；`slice|substring|substr|.payload`
  全文件零匹配（两条硬原则保持）。无新依赖、未触碰全局环境。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-9-report.md`（同文件追加）。

- **Task 9 完成（上下文装配器）**：新增 `src/kernel/context-assembler.ts` 与 `src/kernel/context-assembler.test.ts`
  （简报代码逐字转写）。`assemblePrompt` 只把节点 `consumes` 声明的产物 summary + refs 放进 prompt，
  **payload 正文永不进 prompt**；超限时从数组末尾（最早产出、优先级最低）**整体丢弃**该产物（该产物块**整块从
  「输入材料」节移除**，仅其 refs 归入「被省略的输入材料」节），**全程无 `slice`/`substring`/`substr` 等截断调用**；
  `estimatedTokens = ceil(systemPrompt/4) + ceil(prompt/4)`。TDD：RED = `Failed to load url ./context-assembler.js`
  （模块不存在，非语法错误）→ GREEN = 7/7 通过；全量 75/75 通过，`tsc --noEmit` 退出码 0。
  实测 base prompt（无产物）= 1123 字符 ≈ 284 token，远小于超限用例的 1500，故「丢弃后仍超限」的连续丢弃链成立。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-9-report.md`。
  **（已回退）原实现曾给 `AssembledPrompt` 增补 `worktreePath` 字段并回填入参**——评审裁决不接受（使断言成为
  恒真、字段零信息量、全仓无消费者），已在下方「Task 9 评审后修复」中回退为简报的 5 字段形状。

- **Task 8 评审后修复（2 处：1 Important + 1 Minor）**：`src/runner/fake-runner.ts` ① `run` 的队列耗尽从
  `?? [{kind:'exited',code:0}]` 兜底改为**抛错**（`:44-50`）——原兜底把「脚本份数 < run 次数」这一测试编排错误
  伪装成 `code:0` 成功流（`createFakeRunner({script:[...]})` 只入队 1 份，是最常见用法，Task 12 跑 ≥2 次 run
  必然踩中），且该分支原有 5 条用例**零覆盖**；② `FakeScriptItem` 改为 `Exclude<RunnerEvent, {kind:'started'}>`
  （`:4`），删除 `yield item as RunnerEvent` 断言，`RunnerEvent` 字段漂移将由编译器报错。
  新增护栏用例 `src/runner/fake-runner.test.ts:69-80`。**变异验证**：把兜底改回后护栏**变红**
  （1 failed | 5 passed，输出打印出静默成功流 `[started, {code:0}]`），还原后 GREEN。
  `Exclude` 等价性经**双向类型探针**证实（互相可赋值；探针自检：多加一个变体即报 TS2344，证明探针非恒真），
  临时探针文件已删除。单文件 **6/6**、全量 **68/68**、`tsc --noEmit` exit 0。`types.ts` 零改动，无新依赖。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-8-report.md`（同文件追加）。

- **Task 8 完成（Runner 接口与 Fake Runner，commit `9ccfd20`）**：新增 `src/runner/types.ts`、
  `src/runner/fake-runner.ts`、`src/runner/fake-runner.test.ts`（简报代码逐字转写，3 files，+163）。
  对外暴露 `AgentRunner` / `RunRequest` / `RunnerEvent` / `RunnerCapabilities` 归一化接口，与
  `createFakeRunner(options)`（`FakeScriptItem[]` / `FakeScriptItem[][]` 两种脚本形态）。这是**内核可测性的支点**：
  Task 12 的内核测试将基于 Fake Runner，在不调用真实 API 前提下确定性验证编排逻辑。
  `FakeRunner` 自动补 `started`（`pid:4242`、`sessionId:fake-session-<runId>`）后按脚本回放，队列耗尽兜底
  `{kind:'exited',code:0}`；`requests` / `cancelled` 供断言。**刻意保持 `cancel` 极简**（仅 `cancelled.push`），
  不中断迭代器/不抛错——测试替身越简单越可预测（控制者明确要求）。
  TDD：RED = `Failed to load url ./fake-runner.js`（模块不存在，非语法错误）→ GREEN = 5/5 通过。
  **变异验证**（证明断言非空跑）：① 删 `requests.push` + `queue.shift()`→`at(0)`：2 failed（用例 3、5）；
  ② 不回放脚本：3 failed（用例 1、2、5）；③ `cancel` 不记录：1 failed（用例 4）；三次均已还原，`git diff` 干净。
  全量 **67/67** 通过（9 文件），`tsc --noEmit` exit 0。无新依赖、未改既有文件、未触碰全局环境。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-8-report.md`。

- **Task 7 评审裁决落地（Important：死循环保护语义钉固）**：控制者裁决「只改计划文本、不改代码」——判据是
  「目标节点被反复进入」这件事本身，**刻意不要求该节点曾成功完成**（spec §9.4：`visit_count` 检测同状态反复进入 → 升级）。
  ① `src/kernel/state-machine.ts:79-81` 死循环保护处加中文注释固化该理由（**逻辑零改动**，`git diff` 仅 3 行注释）；
  ② `src/kernel/state-machine.test.ts:194` 新增用例「目标节点反复进入但从未成功完成时，同样判定为死循环」。
  **变异验证**：临时给保护加 `&& state.completedNodeIds.includes(edge.to)` → 新用例变红
  （`expected 'start' to be 'end'`），且**原有 11 条用例全部照常通过**（1 failed | 11 passed）——证明原有用例对该退化
  **零覆盖**，新用例是唯一把守；还原后 GREEN。全量 **62/62** 通过，`tsc --noEmit` exit 0。
  **两处需控制者跟进**：(a) 任务给定的测试片段原样跑**未加变异就已变红**——`completedNodeIds` 为空时 `decideNext`
  走规则 3 直接回 start 节点，到不了死循环保护；已改写为「先让另一节点完成、目标节点 pm_analyze 反复失败 3 次」
  以真正命中保护分支。(b) 计划文档里那句旧文本（「且已在 `completedNodeIds` 中」）已由控制者删除
  （`docs/superpowers/plans/2026-09-18-agentflow-phase0-phase1.md:2390`，commit `ce405fb`）；
  `task-7-brief.md:27` 是编辑前生成的快照，仍含旧文本，以计划文档为准。
- **Task 7 完成（状态机 / 转移求值，commit `ea404b2`）**：新增 `src/kernel/state-machine.ts` 与
  `src/kernel/state-machine.test.ts`（简报代码逐字转写，2 files，+287）。对外暴露 `Decision` 联合类型与
  `decideNext({ workflow, state, facts })`。决策规则按序：终态→end；有运行中节点→wait；无当前/已完成节点→
  start 起始节点；否则取最后完成节点、按声明顺序取第一条匹配出边→start 目标；无匹配边→end/failed。
  **关键语义**：① `edgeMatches` 用 `try/catch` 捕获 `ExpressionError`，把条件求值失败判为
  `{ matched:false }`（错误信息进 `failures` 供诊断），**不向上抛**——沿用 Task 6 裁决（引用未产出产物类型必须抛错，
  若预置空数组会让 `all([])` 空真放行，更危险）；② 死循环保护检查的是**即将启动的目标节点** `state.visitCounts[edge.to]`
  的访问次数（≥3 → end/failed），不是源节点，有反向用例（自环跑 1 次仍允许再进）把守。
  TDD：RED = `Failed to load url ./state-machine.js`（模块不存在，非语法错误）→ GREEN = 11/11 通过；
  全量 **61/61** 通过（8 文件），`tsc --noEmit` 退出码 0。无新依赖、未触碰全局环境。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-7-report.md`。
- **Task 6 评审后修复（2 Important + 1 Minor，已修，commit `d19b2ef`）**：`fix: 补表达式求值器的穷尽性守卫与数组缺字段抛错`
  （2 files，+56/-2）。① **Important** `evalValue` 补 `default: { const exhaustive: never = ast }` 编译期穷尽守卫
  （`src/kernel/expression.ts:307-313`，形式同 `projector.ts:217-220`）；**变异验证**：删 `case 'bin'` 后
  `npx tsc --noEmit` 报 `error TS2322: Type '{ k: "bin"; ... }' is not assignable to type 'never'`（exit 2），
  确认门禁真实生效（此前无守卫时删 case 仍 exit 0）。② **Important** `resolvePath` 数组分支遇「非对象元素」/
  「缺字段」改为抛 `ExpressionError`，不再静默映射为 `undefined`（否则 `any(...)` / `count(...) == 0` / `not`
  会翻到放行一侧，Phase 2 接入真实工作包后可达）；**变异验证**：还原静默映射后新护栏用例变红（`expression.test.ts:71`）。
  ③ **Minor** 补「白名单外的函数名在解析期就被拒」用例（`__functions` 内已存在 `customFn` 仍被拒，证明拒绝来自
  白名单而非注册表查空）；**变异验证**：删掉解析期白名单校验后仅该新用例变红，原 `process.exit(1)` 用例仍通过
  （证实旧安全用例对白名单零覆盖）。`npx vitest run` = **50/50** 通过（expression 12→15 条），`tsc --noEmit` exit 0。
  未改 `tsconfig.json`、无新依赖、未触碰全局环境、未动前轮三项已获认可的裁决。
- **Task 4 评审后修复（3 处，已改）**：① **Important** `append` 原为「先 INSERT 后 `KernelEventSchema.parse`」，
  非法入参（`task_id`/`actor` 为空串能过编译——`NewEvent` 是普通 `string`，schema 要求 `.min(1)`）会留下
  「幽灵行」并推进 `lastSeq()`，且残留行违反 schema，会让 `readAll()` 对**所有**调用方永久抛 `ZodError`；
  已把校验提前到 INSERT **之前**（占位 `seq: 0`）。② **Minor** `append` 返回的 `payload` 由「入参回显」
  改为「落库后重读」——经 `rowToEvent` 与 `readTask/readAll` 同路径，返回值与读出的同一事件严格相等。
  ③ **Minor** 补 `readAll()` 与空库 `lastSeq()` 覆盖。`src/kernel/event-store.test.ts` 5 → 9 条
  （含护栏：非法 `task_id` append 抛错 + `lastSeq()===0` + `readAll()===[]` + 后续写入 seq 从 1 起）。
  **变异验证**：把校验挪回插入之后，护栏与严格一致两条用例变红（`lastSeq()` 实测得 **2**，即两次非法
  append 各留 1 行；继续执行 `readAll()` 在 `rowToEvent` 抛 `ZodError`），证明护栏有效。
  修复后：单文件 9/9、全量 **22/22** 通过，`tsc --noEmit` 退出码 0。公开接口与 DB schema 未动，未装依赖。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-4-report.md`（同文件追加修复报告）。

- **Task 4 完成（事件存储 append-only）**：新增 `src/kernel/event-store.ts` 与 `src/kernel/event-store.test.ts`
  （简报代码逐字转写）。`events` 单表 + `seq` 全局 AUTOINCREMENT（跨任务单调递增），`event_id`/`created_at`
  由库填充，读出走 `KernelEventSchema.parse`；对外仅 `append/readTask/readAll/lastSeq/close`，无改删。
  TDD：RED = `Failed to load url ./event-store.js`（模块不存在，非语法错误）→ GREEN = 5/5 通过；
  全量 18/18 通过，`tsc --noEmit` 退出码 0。**判断点已实测**：`:memory:` 库上 `pragma('journal_mode = WAL')`
  **不报错**，回读为 `[{journal_mode:"memory"}]`（SQLite 忽略内存库设置），该行按简报保留无分支。
  另用一次性脚本（未入库，已清理）验证文件库路径：目录自动创建、`readAll()` 正确、重开库 seq 从 2 续到 3。
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-4-report.md`。

- **Task 3 评审后修复（3 处，已裁决）**：① `artifacts.test.ts` 测试名改为如实描述断言，并新增
  「缺少 `owns` 键被拒」+「`owns: []` 合法（只读语义）」两条隔离用例；② `artifacts.ts` 的 `owns`
  补注释说明空数组=只读（有意为之），`summary` 注释改为「字符上限 4000，500 token 由 Task 9 装配器体现」；
  ③ `package.json` zod 下限 `^3.23.0` → `^3.25.28`（对齐 `zod-to-json-schema@3.25.2` 的 peer），
  lockfile 重装后 zod 仍解析为 3.25.76。`npx vitest run` = 13/13 通过，`tsc --noEmit` 退出码 0。
  控制者裁决**不收紧** `owns` 为 `.min(1)`（只读角色 `owns: []` 是约定语义；Phase 1 不使用工作包）。
- **Task 3 完成（共享领域类型与 Artifact schema）**：新增 `src/shared/{ids,domain,artifacts,events}.ts`
  与对应两个测试文件；补装 `zod-to-json-schema@3.25.2`（国内镜像）。
  `npx vitest run` = 11/11 通过，`npx tsc --noEmit` 退出码 0（`satisfies Record<ArtifactType, z.ZodTypeAny>`
  在 TS 5.9.3 + zod 3.25.76 下无类型问题）。**待裁决**：`WorkPackageSchema.owns` 允许空数组
  （实测空 `owns: []` 判定为合法），测试名对 `owns` 的描述强于 schema 实际约束，matter 归 Task 8；
  详见 `.superpowers/sdd/2026-09-18-agentflow-phase0-phase1/task-3-report.md`。
- **Task 2（Phase 0 CLI 行为探针）评审后修复**：`spikes/cli-probe/README.md` 修正 6 处标注精度问题——
  「实测」定义（含非探针的对照调用）、usage 证据来源限定、`--tools=BogusTool` 改为「未能验证」、
  补 Task 10 的 `--json-schema` gate、「5 行 vs 1 条事件」的字面矛盾、补原始证据留档
  （完整 5 行 JSONL + codex stderr 头尾 + 挂起日志实测计数 **16,403**，原记 14,044 已更正）。
- 原始证据仍在 `/tmp/claude-*.jsonl`、`/tmp/codex-err.txt` 与 `~/.claude/debug/09af1339-*.txt`，未入库（`out/` 已忽略）。

---

## Phase 1 实测结果

> Task 15 端到端冒烟的真实实测数据。脚本为 `tests/e2e/smoke.sh`，**确实在 `mktemp -d` 出来的独立临时 git 仓库里跑**
> （内核 `repoPath = process.cwd()` 落在临时仓库），未在 AgentFlow 仓库根目录跑。
> 本节数据以**最新一轮（权限修复后的重跑验证，PASS）**为准；首轮 FAIL 的原始数据保留在下方「首轮 FAIL 记录」以备对照。

- 执行日期：**2026-09-19**（本机 `date` 实读）
- 端到端结果：**PASS**（`smoke.sh` 末尾输出 `PASS：任务完成`，退出码 0；**只跑了 1 次**，第 44 轮轮询（220s）即读到 `completed`）
- 最终 `status`：**`completed`**，`currentNodeIds` 为空，`completedNodeIds = ["pm_analyze","dev_implement","qa_verify"]`
- 事件总数：**23**（seq 1~23，`task.created` → `task.completed`）
- 节点成功次数：**3 / 3**（`node.succeeded` 3 条；`node.queued` / `node.started` 各 3 条）
- **实测总花费：$0.5876586**（`budgetUsedUsd` —— 用项目自身的 `project()` 重放本次事件库读出，非估算）
- 全程耗时：**213.4s（3 分 33 秒）**（`task.created` 1789794224871 → `task.completed` 1789794438263）
- 目标仓库：`/tmp/agentflow-e2e-target-r7zam3`，taskId `task_b62b5ef08ac344b6afd6`

单节点（耗时取事件库 `created_at` 的 `node.started` → 同节点 `node.succeeded`；花费取 `node.usage_recorded.cost_usd`）：

| 节点 | 状态 | 耗时 | 花费 | tokens (in/out) |
| --- | --- | --- | --- | --- |
| `pm_analyze` | succeeded | 32.8s | $0.054723 | 6627 / 2118 |
| `dev_implement` | succeeded | 70.5s | $0.2042082 | 5059 / 5611 |
| `qa_verify` | succeeded | 110.0s | $0.3287274 | 21865 / 6912 |

事件类型统计（事件库实读，`smoke.sh` 的 `grep -o '"type":"[^"]*"'` 另会把 artifact payload 内的 `"type"` 一并统计，
故脚本输出多出 `requirement` / `code_diff` / `test_report` 各 1 行，属统计口径的已知粗糙处）：

```
   3 transfer.decided
   3 node.queued
   3 node.started
   3 node.usage_recorded
   3 budget.consumed
   3 artifact.created
   3 node.succeeded
   1 task.created
   1 task.completed
```

其他必报项：

| 项 | 实测值 |
| --- | --- |
| 产出 artifact | **3 个，全部 `status: ok`** —— `requirement` / `code_diff` / `test_report` |
| **`dev_implement` 的 `self_test_result`** | **`"passed"`**（关键指标：上一轮角色契约不可满足，只能 `not_run`） |
| `requires approval` 出现次数 | **7**（dev 3 / qa 4 / pm 0；**上一轮 461 次，下降 98.5%**） |
| `log_ref` 指向的文件 | **3/3 全部 `OK`**，无 `MISS` |
| 目标仓库被 agent 改动 | `?? README.md` / `?? data/` / `?? logs/` / `?? scripts/` / `?? tests/` |
| AgentFlow 仓库 | **干净**：`git status --short` 为空（提交前仅 `M` 记录.md 待提交），`git diff --name-only` 计数 0 |
| 全量单测 | **132/132 通过**（18 文件，`npx vitest run`） |

### 权限修复是否真的生效 —— 关键证据

- **dev 真实执行了命令**（从日志 `tool_use` 提取，共 **12 次 Bash 调用**，上一轮 144 次）：
  `chmod +x .../scripts/hello.sh && ls -l ...`、`bash scripts/hello.sh`、`bash scripts/hello.sh | od -c | head -3`、
  `./scripts/hello.sh && bash -n scripts/hello.sh && echo "SYNTAX_OK"` 等；`code_diff` 的 `notes` 亦写明
  「自测：`bash scripts/hello.sh` 输出经 od 校验为恰好 `hello agentflow\n`；`bash -n` 语法检查通过」。
- **qa 真实跑了测试**（共 **17 次 Bash 调用**，上一轮 118 次且全是重试）：自己写了 `tests/test_hello_smoke.sh`，
  用 `bash tests/test_hello_smoke.sh` **跑了 3 轮**，其中一轮是**刻意的变异验证**（把脚本改成 `hello world`、
  去掉可执行位，确认测试会红：`SUMMARY | passed=5 failed=6`），最终一轮 `SUMMARY | passed=11 failed=0`；
  `test_report` artifact 为 `{"suites":["tests/test_hello_smoke.sh"],"passed":12,"failed":0,"failures":[]}`。
- **pm 未调用 Bash**（0 次），`permission_denials` 为空数组。
- **残留的 7 次拒绝**（均为「六类前缀之外」）：`./scripts/hello.sh`（直接按 shebang 执行、不经 `bash`）、
  以及复合命令里夹带的非白名单片段（`; wc -c ...`、`; command -v shellcheck`、`; echo "EXIT=$?"`）。
  dev/qa 都在 1~2 步内改道（如换成 `bash scripts/hello.sh`），**未再陷入上一轮的长时间重试循环**；
  这是 `--allowed-tools` 前缀白名单的已知边界，不是本轮新缺陷。

### 首轮 FAIL 记录（2026-09-19 第 1、2 次，留档对照，问题现已修复）

- 首轮结果：**FAIL**（连跑 2 次，均在 `smoke.sh` 当时的 15 分钟轮询窗口内走不完，`qa_verify` 被窗口到点后的 trap 中断）
- 首轮事件总数：**18**（两次一致）；节点成功次数 **2 / 3**；最终 `status` 恒为 `active`
- 首轮 **实测总花费：$3.39695**（第 1 次 `$1.1678724` + 第 2 次 `$2.2290776`，均从状态接口 `budgetUsedUsd` 实读）

| 运行 | 目标仓库 | `pm_analyze` | `dev_implement` | `qa_verify` | 该次花费 |
| --- | --- | --- | --- | --- | --- |
| 第 1 次 | `/tmp/agentflow-e2e-target-RGORNO` | 20.5s（$0.0388） | 516.5s ≈ 8.6min（$1.1291） | 已启动，约 5.8min 后被中断 | $1.1678724 |
| 第 2 次 | `/tmp/agentflow-e2e-target-KxKgiT` | 16.4s（$0.0273） | 877.7s ≈ 14.6min（$2.2017） | 已启动，约 2.9min 后被中断 | $2.2290776 |

- 首轮遇到的真实问题：**有，3 项**（下述每一条都附原始证据，非推测）

  1. **【配置问题·根因】headless 下 Bash 工具对绝大多数命令被自动拒绝。**
     runner 给可写角色拼的参数是 `--permission-mode acceptEdits --tools=Read,Edit,Write,Grep,Glob,Bash`。
     headless（`-p`）模式**没有人工审批通道**，而 `acceptEdits` 只自动放行**文件编辑**；Bash 里非安全白名单的命令
     一律返回 `This command requires approval`（等同拒绝）。原始日志逐字证据：
     `chmod +x .../hello.sh` → `This command requires approval`；
     `bash scripts/hello.sh` / `./scripts/hello.sh` / `sh ...` / `python3 ...` / `source ...` → 同样被拒；
     `git checkout -b wp/e2e-smoke-hello` → 同样被拒（第 1 次运行里 dev agent 连试 6 次）。
     而 `Read` / `Write` / `Edit` / `ls` / `mkdir` 正常（`File created successfully at: .../scripts/hello.sh`）。
     后果有二：① **与 prompt 直接冲突** —— `backend_dev.md` 要求「完成后必须运行一次自测」、
     `qa_engineer.md` 要求「passed 与 failed 必须来自真实运行结果，不允许估算」，但这两个角色**都跑不了任何脚本/测试**；
     ② agent 会反复换姿势重试被拒命令（第 1 次 dev 共 144 次 Bash 调用、第 1 次 qa 在被中断前 118 次 Bash 几乎全是重试），
     把 8.6~14.6 分钟与 $1.13~$2.20 烧在无效重试上 —— 这是本次单节点花费远超预期
     （预期 $0.05~0.15/节点，实测 $1.129~$2.202/节点，超 8~44 倍）的直接原因。
     **修正方向（本轮未验证、未改代码）**：改用 `--allowed-tools` 预授权（或 `--dangerously-skip-permissions`）等
     真正授予执行权限的方式；仅 `--tools=` 只限定可见工具，不等于授权。

  2. **【脚本缺陷·未改，按简报逐字保留】轮询循环必然跑满 15 分钟，早退分支永不触发。**
     `sed -n 's/.*"status":"\([^"]*\)".*/\1/p'` 里的 `.*` 是贪婪匹配，取的是该行**最后一个** `"status"` 字段；
     `TaskState` 序列化时 `artifacts` 排在 `nodes` 之后，故一旦产出第一个 artifact，读到的一直是 artifact 的
     `"status":"ok"`（实测轮询输出：先 `running`，之后全是 `ok`），`completed` / `failed` 早退分支**永不触发**。
     末段用 `grep '"status":"completed"'` 做的判定本身是正确的 —— 所以是「结论正确、过程空转 15 分钟」。
     对本次的影响是决定性的：一次真实跑需要 pm 16~20s + dev 8.6~14.6min + qa（未知）≈ 明显超过 15 分钟窗口，
     而窗口到点后 `trap` 会 `kill` 掉 server（实测把正在跑的 claude 一并杀掉、无残留进程），于是 qa 永远来不及跑完。

  3. **【已知取舍】内核未对单节点设预算上限**：`RunRequest.budgetCapUsd` 从未被内核赋值（`RoleDef` 也没有预算字段），
     故 `--max-budget-usd` 实际不会传给 CLI，单节点花费不受约束。Phase 1 已把「预算准入」列为后续阶段，属设计内取舍，
     但它是 $1.13 / $2.20 这种单节点花费能出现的前提。

### 首轮因此被真实验证到的部分（正是 Task 12/13 单测未覆盖的那一环）

- ✅ **隔离生效**：两次跑完，AgentFlow 仓库 `git status --short` **只有 `?? tests/e2e/`**（本任务新增），
  **0 个已跟踪文件被改动**；agent 的全部写入都落在临时仓库（第 1 次为 `README.md` `data/` `logs/` `scripts/` `tests/`）。
- ✅ **`log_ref` 真的指向存在的文件**：两次各输出 2 行 `OK`（形如 `<目标仓库>/logs/runs/<run_id>.jsonl`），**无 `MISS`**。
  即内核 `log_ref` 与 runner 实际落盘路径确实相等 —— Task 13 用 `tsx --eval` 比对的接线不变式，这次在真实调用下复核通过。
- ✅ **状态机与流转**：两次都产出 3 条 `transfer.decided`（`decidedBy` 全为 `rule`）、3 条 `node.started`、3 条 `node.queued`；
  `pm_analyze → dev_implement` 的边条件 `all(artifacts.requirement.status == 'ok')` 在真实事实下求值为真并放行。
- ✅ **真实结构化产出**：`requirement`、`code_diff` 两个 artifact 的 payload 都是符合 schema 的对象
  （`parseArtifactPayload` 校验通过），证明 `--json-schema` + `result.structured_output` 通道在真实调用下可用。
- ❌ **首轮未验证（已在重跑轮补上）**：`qa_verify` 的 `node.succeeded` 与 `test_report` artifact 首轮**从未被观测到**，
  「3 个节点全部成功 → 最终 `completed`」这条终极链路**首轮没有跑通**（`tests/e2e/README.md` 的通过标准 2/3/4 条当时未满足）；
  ✅ **重跑轮已全部满足**，见本节开头。

### 复跑方式与原始产物留档

```bash
bash tests/e2e/smoke.sh          # 需先 chmod +x；真实调用 claude，产生真实费用
```

- **重跑轮（PASS，2026-09-19）**：完整 stdout `/tmp/agentflow-e2e-smoke-output-3.txt`；
  目标仓库 `/tmp/agentflow-e2e-target-r7zam3`（含事件库 `data/e2e.sqlite` 与 3 个 `logs/runs/*.jsonl`）。
- **首轮（FAIL）**：完整 stdout `/tmp/agentflow-e2e-smoke-output.txt`（第 1 次）与 `/tmp/agentflow-e2e-smoke-output-2.txt`（第 2 次）；
  目标仓库 `/tmp/agentflow-e2e-target-RGORNO` 与 `/tmp/agentflow-e2e-target-KxKgiT`；server 启动日志 `/tmp/agentflow-e2e.log`。
- 以上证据**均未入库**（临时目录，非本仓库文件）。

---

## 未完成 / 待验证

| 项 | 说明 |
| --- | --- |
| **`owns` 路径级强制（Phase 1 缺口，2026-09-19 终审登记）** | Phase 1 **未实现**。`owns` 只有两个作用：① 拼进 prompt 的硬约束段落（`src/kernel/context-assembler.ts`）；② 以 `role.owns.length === 0` 粗粒度决定该角色是否只读（`src/kernel/kernel.ts` → runner 走只读分支还是可写分支）。**对可写角色而言 CLI 层是全量放行的**（`--allowed-tools` 无路径约束，见 spec §11.3）；内核层无变更路径核对、`artifact.invalidated` 事件**全仓无生产者**。端到端已实测越界：`config/roles/backend_dev.yaml` 的 `owns` 为 `["src/**"]`，agent 却真实写了 `README.md` 与 `scripts/hello.sh`，任务仍正常 `completed`。**因此 `owns` 不得被当作安全边界**；调度前占用检查 + 越界写记 `artifact.invalidated` 属 Phase 2（spec §7.2 / §15 已同步标注） |
| **并发调度未实现（Phase 2，2026-09-19 登记）** | `AGENTFLOW_GLOBAL_CONCURRENCY`（`.env.example` 默认 4）被 `src/config/env.ts` 解析为 `env.globalConcurrency`，但**全仓无消费者**；内核是单节点串行（`src/kernel/kernel.ts` 的 `await runNode(...)`），故管理台「并行度」（`web/src/App.tsx`，取 `currentNodeIds.length`）**恒为 0**——本轮已把该显示项如实标注为「Phase 1 串行执行，并发调度属 Phase 2」，**未实现并发调度本身**（属 Phase 2 规模） |
| ~~Phase 0 探针（必做）~~ | ✅ 已做（2026-09-18）→ 结论见 `spikes/cli-probe/README.md`。**但探针未跑通**：claude 凭据额度不足（403）、codex 请求不返回；结论来自真实进程输出 + 二进制静态验证，逐条标注了可信度 |
| codex 预算控制 | codex 无 `--max-budget-usd` 等价项，需自建统一记账层 |
| 成本预估 | full profile 跑通一个需求的实际 token/费用未知，需 Phase 1 实测 |
| `--json-schema` 的无限循环 | ✅ 已查明边界（2026-09-18）：凭据正常时 7.0s 正常退出；**只在请求持续失败时** CLI 空转不退出（stop hook 反复注入）。Task 10 已带 wall-clock 超时 + 按进程组 kill |
| `result.structured_output` 未运行时验证 | ✅ 已运行时验证（2026-09-18）：字段存在且为符合 schema 的对象，真实样本已归档为 fixture |
| codex 输出流结构未验证 | 实测 30 秒内 stdout = 0 字节、stderr = 61602 字节；最终答案落在哪个流未能验证。**codex 的额度是否随之恢复未复测**（Phase 1 若要用 codex runner 需先补测） |

### 对 Task 10 的结论（从 `spikes/cli-probe/README.md` 复制）

- 解析入口事件类型：`"result"`（实测）
- 结构化结果的字段路径：`result.structured_output`（**运行时实测**，2026-09-18 补跑；仅在传 `--json-schema` 时出现）
- usage 字段路径：`usage.input_tokens` / `usage.output_tokens`（实测）；成本在 `result.total_cost_usd`（实测）
- 最终采用的 claude 调用参数数组（**2026-09-18 修订：加回 `--json-schema`**）：
  `['-p', prompt, '--output-format', 'stream-json', '--include-partial-messages', '--verbose', '--json-schema', <schema>, '--max-budget-usd', '0.50', '--tools=Read,Grep,Glob']`
  （实测 rc=0、15.9s 正常退出；不传 schema 的版本实测会因模型用 ```json 代码块包裹而**零产物**）
- ✅ **gate 已解除**：解析层 `structured_output` 优先、`result` 内 JSON 字符串兜底；
  「永不退出」仅在请求持续失败时出现，超时 + kill 保持。

---

## 阻塞

**无当前阻塞。** ✅ 已解除（2026-09-18）：`claude -p` 真实调用恢复（`is_error:false`）。
原记录如下备查：`~/.claude/settings.json` 曾指向第三方 Anthropic 兼容代理，调用返回
`403 ... 用户额度不足, 剩余额度: ＄-0.044144`（负数），codex 走同一代理时请求长时间不返回；
本轮用户充值/换凭据后 claude 已恢复，**codex 未复测**（见「未完成 / 待验证」表）。
本轮**未修改任何全局配置**。